# ADR-0096: Ціна відкриття свічок FXCM — перший тік (FIRST_TICK), і ремонт значень історії

## Метадані

| Поле | Значення |
| ---- | -------- |
| ID | ADR-0096 |
| Статус | **Accepted** (2026-09-15): слайс A задеплоєно 2026-09-15 21:05 UTC (`db73fab`, перерва ринку, рестарт `smc-fxcm` → `smc-preview` → `smc-ws`; у лозі `FXCM_HISTORY_OPEN_MODE mode=FIRST_TICK`). Слайси B–D пишуть у дані — кожен окреме «го» власника. Слайс E (§3.4, рішення власника 21.09) — у гілці, деплой окремим «го» |
| Дата | 2026-09-14 |
| Автори | Станіслав (owner: звірка XAU D1 з TradingView) + Claude Opus 5 (корінь у SDK, зонд брокера, аудит 18 агентів зі скептиками) |
| Будується на | ADR-0001 (UDS), ADR-0002 (деривація з M1), ADR-0016 (dual venv, FXCM SDK у `.venv37`), ADR-0094 (один вибирач, health v2 root) |
| Пов'язаний | ADR-0095 (зарезервовано: сезонний ремонт derived — якір H4/D1 і календар за сезоном; перебудова похідних TF у цьому ADR впирається в нього), ADR-0054 §3.6 (зависання `get_history`) |
| Поважає | I1, I3, I5 (гучна відмова замість тихого дефолту SDK), X9, X28 (UI не перераховує open) |
| Зачіпає шари | `runtime/ingest/broker/fxcm/provider.py` (єдина точка входу до історії FXCM); дані — M1 і всі похідні TF (слайси B–D); слайс E — `broker_sidecar.py` (команда `fetch_t1`), `m1_ingestion_worker.py`, `m1_session_open.py`, `polling/m1_poller.py`, `config.json:m1_poller.session_open_rebuild` |
| Initiative | `fxcm_open_first_tick_v1` |

---

## Quality Axes

- **Ambition target**: **R2** — свічка на графіку має відкриватися тією ціною, яку трейдер бачить у
  TradingView. Сьогодні перша свічка кожної сесії відкривається вчорашньою ціною.
- **Maturity impact**: **M3 → M4** — режим, що визначав зміст кожного бару, залежав від дефолту SDK і
  хибного коментаря; тепер він явний, один на весь репо, гучно відмовляє без enum, видимий у логах і
  захищений AST-гейтом.

---

## 1. Контекст

### 1.1 Що побачив власник

XAU/USD D1 за 27.07.2026 у TradingView: **O 4090.160 H 4116.185 L 4065.415 C 4076.425**. У нас (бар
`open_time_ms` = 2026-07-26 21:00 UTC): **O 4055.42 H 4116.33 L 4055.42 C 4076.65**. H і C збігаються,
O і L — ні; O=L = закриття п'ятниці. Свічка у TV червона, у нас зелена.

Дата і якір тут правильні: FXCM сам штампує D1 о 21:00 UTC (літо), UI підписує бар торговою датою, як TV.

### 1.2 Корінь

- `forexconnect` 1.6.43: `ForexConnect.get_history(..., candle_open_price_mode=O2GCandleOpenPriceMode.PREVIOUS_CLOSE)`
  за замовчуванням [VERIFIED `.venv37/.../forexconnect/ForexConnect.py:423`, присвоєння `:458`].
- `runtime/ingest/broker/fxcm/provider.py:139` і `:192` параметр не передавали. Коментар поруч:
  «FIRST_TICK mode (default)» — хибний. Історія: явний `PREVIOUS_CLOSE` з першого коміту `8e01f42`
  (04.02.2026); `c7d93eb` (13.02) прибрав kwarg, вважаючи дефолтом FIRST_TICK, — режим лишився той самий
  [VERIFIED git log -S].
- «UI stitching `open[i]=close[i-1]`», на яке посилались доки, у коді не існує: ключ
  `ui_stitching_enabled` ніхто не читає; ws/UI віддають `o` з диска як є.

### 1.3 Виміри

| Що | Результат |
|---|---|
| M1, сусідні хвилини з `o == prev_c` | 99.95–100% у XAU, XAG, NAS100, SPX500, US30, кожен місяць; контроль Binance (справжній open) — 52% |
| Зонд брокера, та сама доба XAU D1 | PREVIOUS_CLOSE: O 4055.42 L 4055.42 · FIRST_TICK: **O 4089.98 H 4116.33 L 4065.25 C 4076.65** (== TV у межах спреду) |
| Зонд, перша M1 сесії | XAU 26.07 22:01: PREV O=L=4055.42 · FIRST O 4089.98 L 4086.33; NAS100 22:00: PREV O 28168.25 · FIRST O 28373.05 |
| Посеред сесії | різниця O 0.01–0.1 |
| Масштаб на похідних | D1 — практично кожен бар починається після перерви; суттєва помилка O у 6–28% D1 (після вихідних 23–66%); хибний L/H у 7.9% D1 (контроль 0.1%); колір D1 перевертається ~5%; H4 — бар 22:00 |
| Живий графік | прев'ю = перший bid-тік, фінал PREV замінює його: 13.09 22:00 NAS100 +387.84, US30 +207.49, XAU +9.72 |
| Гейти | health root порівнює derived з тим самим M1 (PREV з PREV) — дефекту не бачить; `fxcm_raw_compare` — теж PREV з PREV |

Нормалізація `provider.py` (`h = max(o, …)`, `low = min(o, …)`) у режимі PREV підтягувала H або L до
штучного open; зі справжнім першим тіком вона нічого не змінює.

### 1.4 FIRST_TICK справжній не завжди (зонди 14.09, окремий cwd = холодний кеш SDK)

Брокер віддає справжній перший тік лише там, де має тікову історію. На поточному тижні для XAU і NAS100
однаково: **Нд 13.09 22:00 → Пн 14.09 07:00 UTC** — FIRST_TICK «запечений»: o == close попередньої свічки у
100% хвилин, частина open поза [low, high] (XAU 22:01: o 4346.23, h 4337.69). **З Пн 07:00 UTC** — справжній
(o == prev_c 0/60, open у межах), включно з найсвіжішою годиною. Минулі тижні (02.08…10.09, відкриття сесій і
години посеред сесії) — справжні, отже такі проміжки брокер згодом перераховує [INFERRED: момент перерахунку
не виміряно]. Наслідки:
- для живих барів у «запеченому» проміжку FIRST_TICK нічого не виправляє, а нормалізація розтягує H/L до
  такого open — тому провайдер пише `FXCM_OPEN_NOT_FIRST_TICK` (open поза [low, high]) і дані не чіпає;
- ремонт історії (слайс B) бере лише дані, де FIRST_TICK справжній, а «запечені» проміжки — повторно пізніше.

Брокерська історія (`src=history`) теж у режимі PREV, але не вся [VERIFIED ревʼю гілки, скан за полем src]:
- XAU D1 history 1987-06-11..2025-10-13 (9909 барів) + 5 окремих барів 2025-11-25..2026-02-26; ознака PREV
  (o==prev_c) — 99.88% для 2010-01-03..2025-10-13, лише 15.6% для 1987–2009. Derived XAU D1 — з 2025-10-14.
- XAG D1 history 2018-06-06..2026-02-25, з них 96 ключів уже в епосі M1 (41 бар у 2026); derived XAG D1 — з 2026-03-08.
- В епосі M1 (з 2025-10-15) лишились ключі, де є лише history-бар: M5 XAG 23 166, M5 XAU 953, H1 XAG 14.

---

## 2. Альтернативи

| # | Варіант | Чому ні / так |
|---|---|---|
| A | Лишити PREVIOUS_CLOSE, «це конвенція брокера» | Істина проєкту — TradingView; перша свічка сесії бреше і в O, і в L/H, D1 змінює колір. Відхилено. |
| B | Склеювати/розклеювати open у UI | X28: фронтенд не перераховує дані; до того ж справжній перший тік з PREV-бару не відновити. Відхилено. |
| C | Режим як ключ `config.json` | Це не налаштування, а інваріант коректності: значення PREVIOUS_CLOSE ніколи не правильне. Ключ дає спосіб тихо повернути дефект. Відхилено. |
| D | Будувати open фінальних барів з тіків | Тіки проходять дросель relay 200 мс і бувають відсутні (SPX500 без тіків); історія з брокера лишилась би PREV. Відхилено. |
| **E** | **Константа FIRST_TICK в єдиній точці входу, гучна відмова без enum, лог режиму, AST-гейт; ремонт історії окремими слайсами** | **Обрано.** |

---

## 3. Рішення

### 3.1 Код (слайс A)

- `provider.py`: `OPEN_PRICE_MODE_NAME = "FIRST_TICK"`; `_resolve_open_price_mode()` бере enum з
  `fxcorepy.O2GCandleOpenPriceMode`, без нього — `RuntimeError FXCM_OPEN_PRICE_MODE_UNAVAILABLE` при
  створенні провайдера; єдиний виклик SDK `_get_history(...)` з `candle_open_price_mode`; при вході в сесію
  `FXCM_HISTORY_OPEN_MODE mode=FIRST_TICK`; open поза [low, high] у батчі — `FXCM_OPEN_NOT_FIRST_TICK` (скільки
  барів, перший і останній open_ms), дані не змінюються (§1.4).
- Гейт `tests/test_fxcm_open_price_mode.py`: у `runtime/ tools/ app/ core/` виклик `.get_history(` є лише
  в провайдері і лише з явним режимом. Усі шляхи до історії (sidecar `fetch_m1` → m1_ingestion_worker,
  live_recover, tail_catchup, `repair_m1_gaps`, `fetch_tf_backfill`, `htf_*`) йдуть через провайдер.
- Хибні доки виправлено (`runtime/ingest/README.md` включно зі «швидкою перевіркою», `runtime/ingest/polling/README.md`
  §2.2/§15.1/§16, `docs/config_reference.md` — `ui_stitching_enabled` мертвий ключ, `docs/system_current_overview.md`,
  позначки в ADR-0001).
- Споживачі, що мовчки спирались на PREV (знайдені адверсаріальним ревʼю гілки):
  - `core/smc/swings.py compute_atr`: TR = `max(h, prev.c) − min(low, prev.c)` замість `h − low`. На PREV-даних
    результат той самий (H/L уже містили prev.c); з першим тіком без цього ATR на відкриттях сесій менший за TV
    (H1 до −25%, M5 до −52% у вікні після недільного відкриття) → нижчий поріг displacement, інша сила FVG.
  - `runtime/ws/candle_map.py` (рішення власника **21.09**, скасовує 15.09): display ховає ЛИШЕ бари з явним
    маркером `calendar_pause_flat`; правило «flat + v ≤ 10 → сховати» та HTF-виняток за днем тижня прибрано.
    Після FIRST_TICK однотікова торгова хвилина законно пласка, і геометричний фільтр видаляв справжні часові
    точки (вимір 21.09 на проді: EUSTX50 5287/106293 M1 ховалось, 4.97%, з них 5156 у сесії; GER30 675; плюс
    смуга v 5..10, яку інжест взагалі не вважає пласкою — split-brain порогів 4 проти 10). Контроль: Binance
    зберігає пласкі бакети і в klines, і в uiKlines; у LWC плаский OHLC — валідний data point. Існування бару
    визначає upstream-класифікація (m1_session_filter), display не перекласифіковує.

### 3.2 Рейка (слайс A4, окремо)

Health-вимір «частка сусідніх M1 з `o == prev_c` за місяць» → RED вище порогу, каліброваного на даних
FIRST_TICK. `HEALTH_MEASURE_VERSION` 3, baseline перезняти. До ремонту історії всі 5 символів чесно RED.

### 3.3 Дані (слайси B–D)

- **B — M1 value-only.** Перезабір M1 з брокера у FIRST_TICK у staging поза `data_v3` (~125–138 викликів,
  дедлайн на виклик, лише закритий ринок). Заміна лише `o/h/l` наявних ключів; `c` мусить збігтися в межах
  eps, новий діапазон ⊆ старого, нових ключів немає; доказ на копії; запис при реально зупинених записувачах
  (перевірка відкритих FD, не прапорцем), tar + sha-маніфест, `rewrite_atomic`. Свічка, що стала O=H=L=C у
  торгову за календарем хвилину, отримує `extensions.trading_flat`, як від live-інжесту (на графіку вона
  однаково схована — §3.1). Рядок брокера з open поза [low, high] (§1.4) не є першим тіком:
  ключ лишається в плані як «не доведено» і перезабирається пізніше, а не записується з розтягнутим H/L.
- **C — похідні TF.** Перебудова з виправленого M1 сезонними зрізами: індекси й літо металів одразу, зима
  металів — лише після ADR-0095 (поточний `rebuild_from_m1` бере один якір і календар для будь-якої дати).
- **D — брокерська історія (`src=history`).** XAU D1 history 2010-01-03..2025-10-13 (+5 окремих барів 2025-11..2026-02),
  XAG D1 history 2018-06-06..2026-02-25: перезабір значень або мітка legacy. XAU D1 1987–2009 не чіпати без проби.
  History-ключі всередині епохи M1 (M5 XAG 23 166, M5 XAU 953, H1 XAG 14, XAG D1 96) — власник один: або C
  (перебудова з M1 замінює їх derived-барами), або D (перезабір значень); вирішити до запуску, щоб жоден ключ не
  лишився без власника і не мав двох.

### 3.4 Перша хвилина після перерви — з тікової історії брокера (слайс E, рішення власника 21.09)

**Проблема** (живі перевірки 15–21.09): у свіжих добах брокер віддає ПЕРШУ M1 після кожної перерви (денна
21:00–22:00 UTC для XAU/XAG/NAS100/SPX500/US30, EUSTX50 20:00→06:00, вихідні) «запеченою»: open (і high або low)
= close перед перервою. Полер комітить бар один раз (watermark, `commit_final_bar` відкидає повтор), тож
запечене лишається назавжди → гігантська перша свічка сесії на M1…D1; брокер виправляє лише у тижневому
перерахунку. Зонд `get_history(symbol, "t1", start, end)` ТОГО Ж ДНЯ: лише справжні тіки (EUSTX50 21.09 06:01 —
10 тіків від 6281.63, а m1 o=l=6239.79, v=10; XAU 20.09 22:01 — перший тік 4375.62, m1 o=h=4380.77, v=388;
NAS100 22:00 — 29644.01 проти o=h=29677.82).

**Чим це не альтернатива D (§2).** D відхилено через тіки relay (дросель 200 мс, пропуски, SPX500 без тіків).
Тут джерело — тікова історія брокера (серверна), лише для 1 хвилини на сесію, і лише для запечених компонентів:
значення, яке дорівнює close перед перервою, свідомо хибне, тож перший тік хвилини — строго краща оцінка.

**Замір на T+8 с (21.09 15:34–15:36 UTC, окрема сесія, `/tmp/p5/t1_latency_probe.py`)** — t1 за [хв, хв+59.999 с]
проти m1 FIRST_TICK тієї ж хвилини: вибірка 0.4–1.2 с; XAU 538/557 і 586/589 тіків/v — o/h/l/c збігаються, але
15:36 — 1190 тіків при v=818 і h 4355.95 ≠ 4355.75; EUSTX50 22/24, 86/87, 11/39, close тіку 6327.63 ≠ 6328.13;
NAS100 1913/1999, 2196 > 1461, open 30299.71 ≠ 30299.59. Тобто на момент коміту t1 і m1 брокера не узгоджені ні
за кількістю, ні за close (пізні тіки кінця хвилини). Перша версія слайса вимагала «close ≤ ½ кроку» і «обсяг ±5» —
на цих даних майже кожне відкриття пішло б у `open_provisional` (EUSTX50 — щоранку), фікс не працював би.
Тому звірку «тіки = бар» прибрано: перебудовується лише те, що доведено запечене.

**Рішення:**
- Провайдер: `fetch_t1_bid_ticks(symbol, from_ms, to_ms)` через той самий `_get_history` (гейт §3.1 — один виклик
  SDK, явний режим); ціна Bid, як у M1 (у свічці SDK лише Bid*/Ask*). None = відмова SDK, [] = тіків немає.
- IPC: команда sidecar `fetch_t1` (вікно лише 0 < to−from ≤ 1 хв, запобіжник 20000 тіків), той самий
  per-request реплай, BLPOP 15 с, LLEN-гейт, LoopWatchdog головного циклу. Відмова t1 — error-реплай БЕЗ
  перепідключення сесії (reconnect рве OFFERS і fetch_m1 усіх символів); виняток «сесії немає» — reconnect.
- Запечення: бар «першого після перерви» запечений, якщо `o == prev_close` (±½ кроку ціни), де prev_close —
  close останнього закоміченого M1 символу (бар на watermark; після рестарту — з хвоста диску). Не запечений →
  бар як є, t1 не запитується, без WARN (INFO `FXCM_SESSION_OPEN_OK reason=open_not_baked`).
- Чиста функція `m1_session_open.rebuild_session_open_bar(bar, ticks, price_step, prev_close)` для запеченого:
  `o` = bid найранішого тіку з [open_ms, open_ms+60 с); `h` = `bar.h`, якщо він ≠ prev_close, інакше
  max(тіки, c); `low` — дзеркально; нормалізація h ≥ max(o, c), low ≤ min(o, c); **c і v — брокерські без змін**.
  Гейти лише: ≥1 тік у вікні хвилини; перший тік ≠ prev_close (якщо = — справжнє відкриття на тій самій ціні:
  бар як є, без provisional). Маркери `session_open_rebuilt`, `open_before`, `high_before`/`low_before` (якщо
  значення змінилось).
- Полер: для бару «першого після перерви» (попередній закомічений M1 старший за `gap_min` = 15 хв АБО календар:
  хвилина торгова, попередня — ні) і новішого за watermark — перевірка запечення, для запеченого запит t1 і
  перебудова ДО `commit_final_bar` і ДО правила M1→SSOT (`classify_m1_for_ssot` бачить перебудований бар).
  Тіків не отримано / 0 тіків / немає кроку ціни / невідомий prev_close → бар брокера з `open_provisional` +
  WARN `FXCM_SESSION_OPEN_BAKED reason=…`.
- SSOT: `config.json:m1_poller.session_open_rebuild` (enabled, gap_min, price_step_by_symbol);
  K5-гейт `m1_poller.session_open_rebuild → 0096`.

**Залишковий ризик [INFERRED]:** замір T+8 с робився посеред сесії; чи тікова історія ПЕРШОЇ хвилини після
перерви так само доступна на T+8 с — перевіряється на першому відкритті після деплою. Якщо ні — `open_provisional`
гучно (дані не гірші за сьогоднішні), і тоді потрібна відкладена перебудова (окремий слайс). Перший тік t1 на T+8 с
може відрізнятися від FIRST_TICK брокера на кілька кроків (NAS100 30299.71 проти 30299.59) — для запеченого бару це
все одно на порядки ближче за close перед перервою.

---

## 4. Наслідки

**Позитивні**: перша свічка сесії відкривається ринковою ціною, як у TV; D1/H4 отримують правильні O і L/H;
живий графік перестає стрибати на відкритті сесії; режим більше не залежить від дефолту SDK.

**Негативні / ризики**
- Між свічками з'являться видимі розриви (так і в TV) — склеювання не вмикати.
- Епохи змішані: після деплою A нові бари FIRST_TICK, історія PREV — до слайса B. Рейка A4 тримає це RED.
- Після B health root покаже розбіжність O на всіх похідних, доки не пройде C.
- SMC-рівні й тези Арчі, що спирались на хибні O/L/H, зсунуться.
- Малообсягові M1 можуть стати O=H=L=C (flat-фільтри `m1_poller.py:58-59`, `candle_map.py:40-60`) —
  виміряти пілотом.
- Вотермарка (`uds.py:771-795`) не дасть звичайному інжесту перезаписати історію — тому B окремим інструментом.
- Перемальовування прев'ю посеред сесії спричиняє дросель relay 200 мс, не режим — FIRST_TICK його не прибере.

---

## 5. Verification

1. **A локально**: `tests/test_fxcm_open_price_mode.py` — 5 тестів, усі падають на `main` (режим не передається ×2,
   немає відмови без enum, немає логу, гейт знаходить `provider.py:139,192`); `tests/test_first_tick_consumers.py` —
   8 тестів, 2 падають на `main` (TR з розривом угору і вниз), 6 — контролі (зокрема графік ховає пласкі свічки, як і до ADR). Повний набір
   на ЧИСТОМУ checkout гілки: див. changelog цього ADR (робоче дерево містить чужі невідстежені тести).
2. **A на проді (після «го»)**: деплой при закритому ринку; у логах `FXCM_HISTORY_OPEN_MODE mode=FIRST_TICK`;
   у неділю після відкриття — частка `o == prev_c` у перших ≥60 M1 ≪ 0.99; 3 бари звірити з окремим зондом
   FXCM FIRST_TICK; перша M1 сесії не дорівнює закриттю п'ятниці.
3. **B/C**: на копії — геометрія ідентична, `c` незмінний, open-рейка RED→GREEN, TAIL == RANGE; на проді —
   `--compare`, served == disk, 5–10 D1 проти FXCM FIRST_TICK і TV.
4. **E**: `tests/test_fxcm_session_open_rebuild.py` (провайдер t1, протокол через справжній обробник sidecar,
   перебудова запечених компонентів на числах зондів 21.09 — EUSTX50/XAU/NAS100, незапечений бар, тіки > v, полер
   до коміту, prev_close з диску після рестарту, SSOT-конфіг). На проді — на найближчих відкриттях (22:00 UTC,
   EUSTX50 06:00): у лозі `smc-fxcm` на кожен символ рівно один `FXCM_SESSION_OPEN_REBUILT` / `FXCM_SESSION_OPEN_OK` /
   `FXCM_SESSION_OPEN_BAKED reason=…`; збережена перша M1 з `session_open_rebuilt` має open ≠ close перед перервою.

---

## 6. Rollback

- A: `git revert` коміту провайдера, рестарт `smc:smc-fxcm`. Бари, записані у FIRST_TICK, правильні — їх не відкочувати.
- B: зупинити записувачі, повернути `.bak`/tar за маніфестом, рестарт ws.
- C: `.bak` part-файлів або tar зрізу.
- E: `m1_poller.session_open_rebuild.enabled=false` + рестарт `smc:smc-fxcm` (поведінка до слайса); повний відкат —
  `git revert` комітів слайса. Перебудовані бари (open = перший тік замість close перед перервою) не відкочувати.

---

## 7. Рішення власника

1. Прийняти ADR-0096 і вікно для деплою A (рестарт `smc-fxcm`, закритий ринок).
2. Дозвіл і вікна на перезабір M1 (B); чи замінювати `v`.
3. Порядок B/C: M1 і похідні одного зрізу разом чи спершу весь M1.
4. Історія до M1 (D): перезабір значень чи мітка legacy (YELLOW/RED).
5. Прийняти видимі розриви між свічками і зсув SMC-рівнів/тез Арчі.

---

## Changelog

- 2026-09-21 — **слайс E, редизайн**: замір T+8 с (§3.4) показав, що t1 і m1 на момент коміту не узгоджені за
  кількістю тіків і close → гейти «close ≤ ½ кроку» і «обсяг ±5» відкидали б майже кожне відкриття. Тепер
  перебудовуються лише запечені компоненти (o = prev_close → перший тік; h/low = prev_close → з тіків і c); c і v
  брокерські; звірки тіків з баром немає; `max_volume_deficit` прибрано з конфігу.
- 2026-09-21 — **слайс E** (§3.4, рішення власника лагодити «графік як TV»): перша M1 після перерви перебудовується
  з тікової історії брокера до коміту; команда sidecar `fetch_t1`, чиста `m1_session_open`, полер, конфіг. У гілці,
  не задеплоєно. Попутно: `extract_open_time_ms` губив 1 мс на часі з мілісекундами (float над 2^53) — цілочисельне
  ділення.
- 2026-09-21 — власник (після зовнішнього аудиту): «виправити ситуацію з графіком». Корінь «рваного» M1 —
  display-фільтр за геометрією: після FIRST_TICK він видаляв справжні однотікові хвилини (EUSTX50 4.97% M1).
  `candle_map` тепер ховає лише явний маркер `calendar_pause_flat` на всіх TF; пороги й weekday-евристику
  прибрано. SSOT не змінювався.

- 2026-09-15 22:07 UTC — **жива перевірка першої сесії після деплою**: `first_tick_live_check verify 2026-09-15T22:00 6` —
  XAU, XAG, NAS100, SPX500, US30 усі PASS (збережені M1 == FXCM FIRST_TICK, 5–6 барів, `broker_open_not_tick=0`);
  `FXCM_OPEN_NOT_FIRST_TICK` після деплою 0; помилок sidecar 0; глибина FIRST_TICK M1 — щонайменше з 2025-10-15.
  ⚠ Перша хвилина після денної перерви в усіх 5 символів має open == close сесії. Зонд тієї ж хвилини в минулі дні:
  01–10.09 open ≠ close (XAU/XAG/SPX500/US30 6/6, NAS100 5/6), 14.09 і 15.09 — рівно (5/5). Отже свіжі доби брокер
  віддає з «запеченою» першою хвилиною (§1.4), решту хвилин сесії — справжніми. Живий інжест зберігає те, що віддав
  брокер, тож ці хвилини потребують перезабору після перерахунку брокером: перевірити 14–15.09 22:00/22:01 ще раз
  через кілька діб і, якщо перераховано, включити свіжі тижні в слайс B.
- 2026-09-15 21:05 UTC — **Accepted, слайс A на проді** (`db73fab`, ff-merge гілки в main). Вікно перерви 21:00–22:00:
  після SSOT-дедупу 97 файлів (ADR-0094 P1.6) зупинені `smc-fxcm`/`smc-preview` запущено з новим кодом; у
  `broker_sidecar.err.log` `FXCM_HISTORY_OPEN_MODE mode=FIRST_TICK`, `M1_POLLER_REDIS_PRIME symbols=5 primed_bars=187549`;
  спостереження 120 с: усі RUNNING, ERROR/Traceback 0. Повний прогін на чистому checkout `db73fab`: 1449 passed, 6 skipped.
  Жива перевірка першої сесії (22:00 UTC) — окремим записом.
- 2026-09-15 — власник: «TradingView не показує пласкі бари». Зміну `candle_map` (показ `trading_flat`) відкочено до
  деплою: графік ховає пласкі свічки v ≤ 10, як до ADR-0096. Вимір на засіві FIRST_TICK: GER30 ~1%, EUSTX50 8.8%
  хвилин пласкі; на дисках живих символів (ще PREVIOUS_CLOSE) пласких 0–31 за 60 діб.
- 2026-09-14 — власник: «го» на деплой слайса A у вікні 14.09 21:00–22:00 UTC після дедупу. Зонди: FIRST_TICK
  «запечений» Нд 22:00 → Пн 07:00 UTC поточного тижня (§1.4) → рейка `FXCM_OPEN_NOT_FIRST_TICK`.
- 2026-09-14 — адверсаріальне ревʼю гілки (15 агентів): 7 підтверджених S3, блокерів деплою немає; закрито в гілці —
  ATR з попереднім close, `trading_flat` у candle_map, залишки доків про stitching, точні межі history за src. Повний набір на чистому checkout гілки (`70e7bfa`): 1446 passed, 6 skipped.
- 2026-09-14 — створено (Proposed): звірка власника з TV, корінь у дефолті SDK, зонд брокера PREV vs FIRST_TICK,
  аудит 18 агентів зі скептиками; слайс A (код + гейт) у гілці `fix/fxcm-first-tick`.
