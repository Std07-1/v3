# Runtime dependencies — platform venv (Python >=3.11, ADR-0016)
redis==5.0.1
numpy>=1.26
pandas>=2.1
python-dotenv>=1.0,<2.0
aiohttp>=3.9,<4.0
python-binance>=1.0.36

# Public read-only API sidecar (ADR-0058, slice 058.1)
fastapi>=0.136.1,<1.0
uvicorn[standard]>=0.27,<1.0

# FXCM ForexConnect SDK — НЕ інсталюється у цей venv.
# Встановлюйте у .venv37/ через requirements-broker.txt
# Див. docs/compliance/fxcm-sdk-license-review.md
