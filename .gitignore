# ============================================================
#  .gitignore — Trading Platform v3
#  Правило: в репо лише код + конфіг + core docs + тести.
#  Все інше (дані, аудити, спеки, IDE, build output) — тут.
# ============================================================

# ── Python ───────────────────────────────────────────────────
.venv/
.venv37/
__pycache__/
*.pyc
*.pyo
*.egg-info/
.mypy_cache/
.pytest_cache/
.ruff_cache/
patch.diff
_tmp_jsonl_stress

# ── Секрети ──────────────────────────────────────────────────
.env
.env.local
.env.prod

# ── IDE / Editor ─────────────────────────────────────────────
/.vscode/
/.cursor/
/.antigravity/
# .github/ — НЕ ігнорувати! Містить CI, dependabot, copilot-instructions
/.github/.agent/
/.idea/
*.swp
*.swo
*~
.agent/
Untitled-*.txt

# — Claude Code (local agent config + memory) ————————
#.claude/
#CLAUDE.md

# — Copilot customization (local) ————————————————————
/.copilot/

# ── Дані (runtime, не код) ──────────────────────────────────
/data_v3/
/History/
/exports/
dump.rdb
tikcets/
artifacts/
/data_v3.local_pre_sync_20260505-093317/
/data_v3.vps_backup/

# ── Node / UI build artifacts ───────────────────────────────
node_modules/
ui_v4/node_modules/
ui_v4/dist/

# ── Legacy ───────────────────────────────────────────────────
/Old system Connector v2/

# ── Research / аудити / звіти (dev-only, не релізні) ────────
/research/
/reports/

# ── Trader Bot (PROPRIETARY, не в репо) ─────────────────────
/trader-v3/

# ── Журнали, стан, діагностика ──────────────────────────────
# changelog.jsonl — трекується (потрібен для роботи з інших машин)
/rebuild_state.json
*.log
logs/

# ── Документація: внутрішні аудити та спеки (не релізні) ────
# /docs/adr/
/docs/audit/
/docs/audit-*
/docs/system_spec/
/docs/claude_project_instructions.md
/docs/Аналітичний меморандум.md
/docs/Host Contract - Capability Sheet.md
project_instructions.md
rules.md


# ── UI v4: planning/spec docs (не релізні) ──────────────────
ui_v4/system_spec_ui4/
ui_v4/UI_v4_COPILOT_PROMPT.txt
ui_v4/UI_v4_COPILOT_README.md
ui_v4/UI_v4_Addendum_v2_consolidated.md
ui_v4/UI_v4_Addendum_v3_consolidated.md
ui_v4/UI v4 _v3.md
ui_v4/Architecture Review & Slice 0 (Оновлено).md
ui_v4/LWC Engine (з фіксом Dedupe Scrollback).md
ui_v4/.env.example

# ── Root-level аудити/плани (не релізні) ─────────────────────
/ПОВНИЙ АУДИТ AS-IS + TO-BE ADR + ПЛАН.md
план реалізації системи.md

# ── Tools (діагностичні, не core) ───────────────────────────
# tools/diag/ — трекується! gate_unexpected_gap_budget.py робить `from tools.diag.classify_m5_gaps`
# Щоб CI exit-gates не падали, увесь tools/diag/ має бути в репо.
/tools/tail_guard.py
/tools/audit/
/tools/_diag_*.py
/tools/_check_*.py
/tools/_ssh_*.py
/tools/check_candle.py
/tools/check_data.sh
/tools/check_m5_derive.py
/tools/scan_broken_bars.py
/tools/purge_rebuild_h4.py
/tools/compare_m5_derived_vs_broker.py
/tools/dedup_jsonl.py
/tools/backfill_cascade.py
/tools/live_cascade_monitor.py
/tools/tail_integrity_scanner.py
/tools/smc-nginx-v3.conf

# ── Великі артефакти (архіви, дампи) ───────────────────────
*.tar.gz

# ── UI v4: old/dead code ────────────────────────────────────
ui_v4/src/chart/ChartPane.svelte
# CommandStack.ts — НЕ dead code: активно імпортується DrawingsRenderer.ts (undo/redo stack)

# ── Python config/packaging (поки не використовується) ──────
/mypy.ini
/pytest.ini
/setup.cfg
/tox.ini
/coverage.xml
/.coverage

# ── Тимчасове ────────────────────────────────────────────────
*.tmp
*.bak
setup_v3.sh

trader-v3/



ui_archi/dist/

# Throwaway debug scripts at root
/_smoke.py
/h?.py
/m??.py


# 2026-04-21 cleanup
ui_archi/dist[0-9]*/
backups/
.playwright-mcp/
_tail*.txt


# === Claude Code (per-machine, runtime cache) ===
.claude/settings.local.json
.claude/agent-memory/
.claude/scheduled_tasks.lock
.claude/.cache/
