"""Брокерські провайдери (FXCM, Binance) і спільні межі IPC з ними.

Модуль імпортують і broker_sidecar (Python 3.7), і полер M1 (Python ≥3.11) — лише константи, без залежностей.
"""

# Стеля барів за одну команду fetch до брокера: sidecar ріже запит до неї (захист від величезних запитів).
# Полер M1 бере з неї розмір сторінки вікна, тож запит, більший за стелю, ніколи не йде — інакше обрізка
# мовчки відкидає найстаріші хвилини гепа (рев'ю dfca437, 23.09.2026).
MAX_BARS_PER_FETCH = 200
